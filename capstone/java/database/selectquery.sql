-- How to use this:
-- Have your cursor select the line you wish to run.
-- Then press F5 or click the Execute Button (should look like a play button.)

--  For example, you hover your cursor over "SELECT * FROM users;""
--  Select it by holding down left-mouse button and dragging it.
--  Then press F5 or click the execute button which will execute: "SELECT * FROM users;"
--  It will display everything on the users table.

SELECT * FROM users;

SELECT * FROM tournaments;

SELECT * FROM tournament_user;

SELECT * FROM matches;